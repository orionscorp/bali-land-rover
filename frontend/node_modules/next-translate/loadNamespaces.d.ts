import { I18nDictionary, LoaderConfig } from '.';
export default function loadNamespaces(config?: LoaderConfig): Promise<{
    __lang: string;
    __namespaces?: Record<string, I18nDictionary>;
}>;
type LogProps = {
    page: string;
    lang: string;
    namespaces: string[];
};
export declare function log(conf: LoaderConfig, { page, lang, namespaces }: LogProps): void;
export {};
